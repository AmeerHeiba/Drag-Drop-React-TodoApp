import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#522258",
    },
    secondary: {
      main: "#8C3061",
    },
    background: {
      default: "#f5f5f5",
      paper: "#fff",
    },
    text: {
      primary: "#C63C51",
      secondary: "#D95F59",
    },
  },
});

export default theme;
